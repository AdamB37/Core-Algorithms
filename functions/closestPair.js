export default function closestPair(points) {
  let result = {pair: [], distance: 99999}

  for(let i = 0; i < points.length - 1; i++) {
    for(let j = 0; j < points.length; j++) {
      
    }
  }
}
